<% if calendar_items.any? %>
  <dl class="highlight">
    <dt>Calendar</dt>
    <dd>
        <ul class="links">
        <% calendar_items.each do |item| %>
          <li>
            <% if p = page_from_setting(:content, :calendar_items_overview_page_id) %>
              <a href="<%= p.url %>">
            <% else %>
              <a name="<%calendar_item.id%>">
            <% end %>
            <strong><%= l(item.calendar_date,:format => :long) %></strong><br />
            <%= item.title %>
            </a>
          </li>
        <% end %>
      </ul>
    </dd>
  </dl>
<% end %>